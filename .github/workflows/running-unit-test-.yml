name: Run unit tests on feature branches

on:
  push:
    branches:
      - 'feature/*'

jobs:
  run-unit-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Run unit tests
        run: mvn test
